const form=$('#registrationForm');
if(form){form.addEventListener('submit',async e=>{e.preventDefault();const btn=form.querySelector('button[type=submit]');btn.disabled=true;btn.textContent='SUBMITTING…';try{const fd=new FormData(form);const payload=Object.fromEntries(fd.entries());payload.events=[...form.querySelectorAll('[name="events"]:checked')].map(x=>x.value);const r=await roboApi('/api/registrations',{method:'POST',body:JSON.stringify(payload)});document.querySelector('#formStatus').textContent=`Registration received. ID: ${r.id}`;form.reset()}catch(err){document.querySelector('#formStatus').textContent=err.message}finally{btn.disabled=false;btn.textContent='SUBMIT REGISTRATION →'}})}
const help=$('#helpForm');if(help){help.addEventListener('submit',async e=>{e.preventDefault();const b=help.querySelector('button');b.disabled=true;b.textContent='SENDING…';try{const payload=Object.fromEntries(new FormData(help).entries());const r=await roboApi('/api/help',{method:'POST',body:JSON.stringify(payload)});$('#helpStatus').textContent=`Support request received. Ticket: ${r.id}`;help.reset()}catch(err){$('#helpStatus').textContent=err.message}finally{b.disabled=false;b.textContent='SEND REQUEST →'}})}
